#include "lexer.h"

/*
 * lexer.cpp - lexer implementation
 */

lexer::lexer(std::string config_file)
{
	/* List used to generate lookup table */
	std::vector<int[256]> lookup_list;

	/* Cycle through symbols that do not change: */
	for (int i = NUM_KEYWORDS; )
	
}

// TODO finish this function
std::vector<lexeme> lexer::lex()
{
	return std::vector<lexeme>();
}
